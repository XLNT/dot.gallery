{
  "version": 2,
  "alias": "dot.gallery",
  "builds": [{ "src": "package.json", "use": "@now/static-build" }],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
