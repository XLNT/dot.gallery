query CurrentEntity {
  currentEntity {
    id
    handle
    availableTicket {
      id
    }
    assets {
      id
      domain
      uri
    }
    tradableAssets {
      id
      domain
      uri
    }
  }
}
